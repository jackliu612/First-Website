const width = 1000;
// const height;
// const padding;
// const size;

function setup() {
	console.log(width)
	// height = 1000;
	// padding = 20;
	// size = 5;

	let myCanvas = createCanvas(width, height);
	myCanvas.parent('canvas');
}

function draw() {
	noStroke();
	fill(22, 111, 255);
	for(var r = 0; r < 5; r++){
		for(var c = 0; c< 5; c++){

		}
	}
}